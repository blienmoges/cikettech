import ProjectForm from "../ProjectForm";

export const metadata = {
  title: "Add Project | CIKETTECH Admin",
  description: "Add a new CIKETTECH project.",
};

export default function NewProjectPage() {
  return <ProjectForm />;
}
