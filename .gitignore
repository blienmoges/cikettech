# Local Claude Code session data — not part of the project.
.claude/

# Each app also has its own .gitignore (node_modules, .env, data.sqlite,
# uploads/, backups/, .next, coverage) scoped to that folder.
